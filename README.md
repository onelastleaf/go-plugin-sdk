# onelastleaf Go plugin SDK

The Go runtime client for trusted onelastleaf process plugins. It owns the
long-lived `PluginRuntime.Connect` stream, handshake, message routing, jobs,
cancellation, host calls, heartbeat, and parent-liveness behavior.

The module path is `github.com/onelastleaf/go-plugin-sdk`.
